name: main

on:
  push:
    branches: [ main ]
  schedule:
    - cron: 0 0 1 * *

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.10'
      - run: python test.py
